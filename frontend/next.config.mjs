/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true, // Optional, often enabled by default
  // output: 'standalone', // Example for Docker deployment
};

export default nextConfig;
