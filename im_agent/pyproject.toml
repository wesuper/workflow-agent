[project]
name = "im_agent"
version = "0.1.0"
description = "macOS Desktop Automation Smart Agent"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0",
    "requests>=2.20",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
